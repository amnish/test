pipeline {
    agent any
    stages {
        stage('Pull the code') {
            steps {
                git url: 'https://github.com/amnish/test.git', branch: 'loginpage'
            }
        }
        stage('Build & Push Docker Image') {
            steps {
                docker build -t app .
                docker -d run app
            }
        }
    }
}
